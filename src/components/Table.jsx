import React, { useEffect, useState } from "react";
import { Grid, Card, CardContent } from "@mui/material";
import { obj } from "./navData";

export const Table = () => {
  const [data, setData] = useState(obj);

  return <Grid container spacing={2}>
    
  </Grid>;
};
